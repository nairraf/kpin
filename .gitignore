# Agent files (memory system, skills, config) — never commit
agents
AGENTS.md

# Vaults are machine-local — never commit keyfiles or DBs
*.kdbx
*.key
*.keyx

# Local pointer files
.kpin

# Python
__pycache__/
*.pyc
.venv/
dist/
build/
*.egg-info/

# Node
node_modules/
