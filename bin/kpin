#!/usr/bin/env python3
"""kpin launcher — adds the repo to sys.path and runs the CLI."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

from kpin.cli import main

if __name__ == "__main__":
    main()
